#pragma once

#include "draw/draw.h"
#include "ui.h"

void runHardware(Draw& d, UiKick& ui, bool& needFullRedraw)
{
    (void)d;
    (void)ui;
    (void)needFullRedraw;
    // Embedded hardware loop placeholder
}
